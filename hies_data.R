

NIH_ige_key <- tibble(cutpoints = c(0,200,501,1001,2001),
                      scores =  c(0,1,4,8,10))

NIH_abscess_key <- tibble(cutpoints = c(0,1,3,5),
                          scores =  c(0,2,4,8))

NIH_pna_key <- tibble(cutpoints = c(0,1,2,3,4),
                      scores =  c(0,2,4,6,8))

# Numeric value based on selectInput key values
NIH_lung_key <- tibble(cutpoints = c(1,2,3),
                       scores =  c(0,6,8))

NIH_teeth_key <- tibble(cutpoints = c(0,1,2,3,4),
                        scores =  c(0,1,2,4,8))

NIH_scolio_key <- tibble(cutpoints = c(0,10,15,21),
                         scores =  c(0,2,4,8))

NIH_frx_key <- tibble(cutpoints = c(0,1,3),
                      scores =  c(0,4,8))

NIH_eos_key <- tibble(cutpoints = c(0,700,801),
                      scores =  c(0,3,6))

# Numeric value based on selectInput key values
NIH_face_key <- tibble(cutpoints = c(1,2,3),
                       scores =  c(0,2,5))

# Numeric value based on selectInput key values
NIH_midline_key <- tibble(cutpoints = c(1,2,3),
                          scores =  c(0,2,5))

# Numeric value based on selectInput key values
NIH_rash_key <- tibble(cutpoints = c(1,2),
                       scores =  c(0,4))

# Numeric value based on selectInput key values
NIH_eczema_key <- tibble(cutpoints = c(1,2,3,4),
                         scores =  c(0,1,2,4))

NIH_uri_key <- tibble(cutpoints = c(0,3,4,7),
                      scores =  c(0,1,2,4))

# Numeric value based on selectInput key values
NIH_candida_key <- tibble(cutpoints = c(1,2,3,4),
                          scores =  c(0,1,2,4))

# Numeric value based on selectInput key values
NIH_serious_key <- tibble(cutpoints = c(1,2),
                          scores =  c(0,4))

# Numeric value based on selectInput key values
NIH_fatal_key <- tibble(cutpoints = c(1,2),
                        scores =  c(0,4))

# Numeric value based on selectInput key values
NIH_hyper_key <- tibble(cutpoints = c(1,2),
                        scores =  c(0,4))

# Numeric value based on selectInput key values
NIH_lymphoma_key <- tibble(cutpoints = c(1,2),
                           scores =  c(0,4))

# Numeric value based on selectInput key values
NIH_nasal_key <- tibble(cutpoints = c(1,2,3),
                        scores =  c(0,1,3))

# Numeric value based on selectInput key values
NIH_palate_key <- tibble(cutpoints = c(1,2),
                         scores =  c(0,2))

NIH_age_key <- tibble(cutpoints = c(0,1.1,2.1,5.1),
                      scores =  c(7,5,3,0))